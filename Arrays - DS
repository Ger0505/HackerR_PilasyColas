#include <stdio.h>
#include <stdlib.h>
#include <string.h>
int main(){
    int N,*A,i;
    scanf("%i",&N);
    A=malloc(N*sizeof(int));
    for(i=0;i<N;i++){
        scanf("%i",&A[i]);
    }
    for(i=(N-1);i>=0;i--){
        printf("%i ",A[i]);
    }
}
